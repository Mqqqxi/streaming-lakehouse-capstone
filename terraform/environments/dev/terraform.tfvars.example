# Copiar este archivo como terraform.tfvars (sin el .example) y poner
# una contraseña real. Con este archivo presente, Terraform toma el
# valor solo -- no hace falta pasar -var="redshift_admin_password=..."
# en cada comando.
redshift_admin_password = "CambiarEstaPassword123!"
